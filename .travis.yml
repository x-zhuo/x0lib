language: python

services:
  - redis-server

python:
  - "3.5"
  - "3.6"
  - "3.7"

  - "pypy3"
  - "nightly"

install:
  - pip install pytest-cov codecov
  - pip install -r requirements.txt

script: python -m pytest --cov=./

after_success:
  - codecov